# Optional: Set this in Vercel Environment Variables so all devices (iPad, phone, laptop)
# can generate articles automatically without needing to enter an API key on each device.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
